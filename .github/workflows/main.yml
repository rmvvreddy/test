name: PR Test Workflow

on:
  pull_request:
    branches: # Runs when a PR is created or updated for `dev or uat`
      - dev 
      - uat

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello from GitHub Actions!"
